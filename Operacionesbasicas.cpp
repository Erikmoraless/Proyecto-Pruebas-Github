    // Division
    if (n2 != 0) {
        rd = (float) n1 / n2;
        cout << "Resultado de la division: " << rd << endl;
    } else {
        cout << "No es posible dividir entre 0." << endl;
    }

    return 0;
}
